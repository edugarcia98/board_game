# Board Game

## Executando a aplicação

Para executar a aplicação, deve-se executar os seguintes comandos:

```shell
pip install -r requirements.txt		# instalar as dependências

python main.py		# executar a aplicação
```

## Executar os testes unitários

```shell
python -m unittest
```
